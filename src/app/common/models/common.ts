export interface Cities {
    name: city[]
}

export interface city {
    name: string
}

export interface Localities {
    name: string
}

export interface Data {
    id: string;
    title: string;
    count: string;
}